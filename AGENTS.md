# TCG 项目维护与执行记录

后续修改此仓库时先读本文件，再读任务涉及的代码与测试。用户于 2026-09-09 明确要求：记住上次耗时和返工的原因，避免重复走弯路。本文件是项目交接记录，不替代用户的新指令或权限要求。

## 当前约定

- 以本地 Python / FastAPI / LangGraph / LangChain / SQLite 和聊天界面为主，不重新引入已经放弃的 Cloudflare 部署。
- 保留 .env、自定义 Header、仅 Header 鉴权、minimal Chat Completions 接入和 3600 秒模型超时。
- 用户已明确不需要迁移旧任务/检查点；保留模型配置、来源和已发布成果，不为旧任务兼容扩大范围。
- 2026-09-08 的增量实现位于 PR #7、分支 codex/incremental-agent。本记录写入时 PR 尚未合并；后续先查实际分支与提交，不把这条状态当作永久事实。
- 当前设计见 docs/INCREMENTAL_AGENT.md 和 docs/AGENT_API.md。历史 v2 指南不能作为新版行为标准。

## 上次为什么耗时过长

以下基于开发记录，不是用户内网模型的性能诊断；没有完整总耗时分解，不估算各环节占比。

1. **一次改动面过大。** 同时调整调度、资料工具、指令版本、评审、持久化、前端进度和文档，较晚才发现这些边界之间的不一致。
2. **关键保留约束验证偏晚。** 首批测试通过后，审查又发现“补充指令丢新范围”“已接受的修改被原始版本覆盖”“跨组路径仍带旧节点”等问题，导致多轮返工。
3. **重复尝试卡住的开发工具。** 有的读取/浏览器调用长时间没有输出；中断后再次使用同一路径，仍未产生有效结果。没有及时止损。
4. **执行环境假设没有先验证。** 独立命令启动服务后，另一个命令访问 localhost 失败。把服务和浏览器放在同一执行环境、检查就绪后再测试才成功。不要假定所有工具共享网络命名空间。
5. **GitHub 操作走了弯路。** 已连接 GitHub 工具，但直接 git 传输仍可能缺少凭据；曾猜测工具参数，或把带完整 diff 的响应直接输出，造成无效调用和巨大上下文。
6. **验证结果回报不够严谨。** 曾在未拿到完整进程退出状态时转述测试通过，随后必须重跑确认。必须看到退出码和最终汇总再报告。
7. **旧测试与新产品语义混淆。** 旧 Auto 等待规则与新要求相反，一开始导致大量旧集成测试失败。保留的 v2 契约测试应明确标为历史验证，不能替代 v3 默认入口验收。

最终一轮后端回归约 107 秒，前端测试约 10 秒，构建约数秒；不能把整个开发等待归咎于测试或“GitHub 提交慢”。

## 后续怎么做

1. **先确认现状与本次目标。** 读取实际 PR/分支、图版本和相关日志，明确本次只解决什么。临时工作区被清理时，从 GitHub 恢复已有代码和交接记录，不要求用户重新上传仓库已有内容。
2. **先跑通最小完整流程。** 用真实 API、LangGraph、SQLite 和受控模型，先验证一组需求从输入到发布；修改恢复逻辑时优先覆盖“已接受结果 + 中途补充要求”。
3. **围绕根因做小批改动。** 每批补一个能复现实际问题的测试，改对应模块，立即跑相关测试；确认通过再扩大范围。不要顺手重做整个架构或新增无关部署。
4. **先针对性验证，后统一回归。** 最终集成时跑完整后端、前端测试与构建。之后仅因新的具体风险或必要门槛扩大复测，不因报告不清楚而重复整套测试。
5. **卡顿要及时改变路径。** 开发工具长时间无输出先检查进程/会话；同一路径重复失败两次后应说明阻塞并换用已授权、可用的方式。不得绕过权限，也不得把此规则用于缩短用户要求的模型超时。
6. **只报告确认过的状态。** 测试成功必须有退出码 0 和完整汇总；子任务的结论也要有证据。过程每约一分钟说明已解决什么、剩下什么、是否遇到工具阻塞。
7. **GitHub 按现有能力操作。** 先读实际工具声明，不猜参数。直接 git 无凭据时用已连接的 GitHub 工具；只输出需要的元数据，不输出完整大型 diff。批量提交可用 tree + commit + ref，确认远端树与本地测试树一致，避免逐个文件产生大量提交。
8. **浏览器测试先检查环境。** 优先使用当前可用且规定允许的浏览器路径；本地替代方式必须确认许可。服务就绪后再访问，失败时区分工具环境问题与产品问题。缺少中文字体、未验证真实模型等限制要如实记录。

## 已踩过的代码边界

| 问题 | 必须保留的约束 |
| --- | --- |
| 新补充只包含新业务模块，被影响分析漏掉 | 新范围独立加入；不改变无关已完成组 |
| 所有工作键带全局指令版本 | 只让受影响的已完成组失效，不能重做全部组 |
| 已接受修改再次修改时回到原始条目 | 使用最新已接受条目为基础；累计 update/add/delete 后再发布 |
| 旧模型回复晚到 | 写入前核对指令版本，过期回复不发布 |
| 长原始段落的引用被展开成全文 | 保留段落引用和 excerpt 偏移；依赖通过 from_units 选择有界片段 |
| 依赖只读原始证据，漏掉澄清覆盖 | 当前来源单元必须包含其有效 override 证据 |
| 同一依赖回复既改规则又生成跨组边 | 先接受新规则/节点，再重新检查边；不把旧节点混入最终图 |
| 评审删掉唯一覆盖用例，或分组类型统计过期 | 对累计已接受结果核查需求/分支/场景和适用类型；失败不覆盖已发布版本 |
| operations 字段修复定位到合并后的 items 路径 | 定位实际 operations[index].item.field，只修该字段 |
| 草稿完成后被当成正式成果 | preview 始终只读，不作为下一任务目标；正式成果单独发布 |
| 直接输入业务却没有来源 | 自动任务复用路由分类；必要时简短分类，并把明确业务正文原样保存 |
| 读取一份资料先加载整段聊天的所有正文 | SQL 先按授权来源过滤；来源授权检查不在每个段落上重复读取整个来源对象 |

## 验证入口和边界

- 后端：PYTHONPATH=backend python -m pytest tests -q
- 前端：在 frontend 运行 npm test 和 npm run build。
- 新版验收：tests/test_incremental_agent.py、test_incremental_workspace.py、test_incremental_regressions.py、test_incremental_intake.py。
- 历史 v2 的四个模块使用 tests/legacy_agent_app.py 显式运行旧解释器；不能把其成功当成新版 Auto、恢复和发布逻辑的证明。
- 2026-09-08 实现交付记录：后端 319 项、前端 45 项通过；这是当时的结果，后续改动必须获取相应新证据。
- 当时的真实浏览器主流程检查覆盖输入、进度、最终成果、Mermaid SVG 和只读草稿；测试环境缺少中文字体，未验证中文字体显示质量。
- 用户内网 AI 的实际速度、排队、Token 用量和业务质量未在此环境验证。受控模型能证明调度和保存约束，不能证明真实模型速度。
