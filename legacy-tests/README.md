# Historical tests

These files describe the v2 controllers and checkpoint contracts retired in v3.0.0.
They remain as implementation history and are excluded from the default test run.
They are not expected to execute against the native Agent + Pipeline runtime.

Run the current architecture tests with `python -m pytest tests` from the project root.
