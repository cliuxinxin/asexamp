import {sqliteTable,text,integer,index,primaryKey} from 'drizzle-orm/sqlite-core';
export const owners=sqliteTable('tcg_owners',{owner:text('owner').primaryKey(),revision:integer('revision').notNull().default(0),gate:text('gate')});
export const records=sqliteTable('tcg_records',{owner:text('owner').notNull(),id:text('id').notNull(),kind:text('kind').notNull(),projectId:text('project_id'),chatId:text('chat_id'),payload:text('payload').notNull()},t=>[primaryKey({columns:[t.owner,t.id]}),index('tcg_scope').on(t.owner,t.kind,t.projectId,t.chatId)]);
export const leases=sqliteTable('tcg_leases',{owner:text('owner').notNull(),runId:text('run_id').notNull(),token:text('token').notNull(),expires:integer('expires').notNull()},t=>[primaryKey({columns:[t.owner,t.runId]})]);
export const events=sqliteTable('tcg_events',{id:integer('id').primaryKey({autoIncrement:true}),owner:text('owner').notNull(),runId:text('run_id').notNull(),kind:text('kind').notNull(),payload:text('payload').notNull(),createdAt:text('created_at').notNull()},t=>[index('tcg_event_run').on(t.owner,t.runId,t.id)]);
export const checkpoints=sqliteTable('tcg_checkpoints',{owner:text('owner').notNull(),thread:text('thread').notNull(),ns:text('ns').notNull(),id:text('id').notNull(),parent:text('parent'),payload:text('payload').notNull(),metadata:text('metadata').notNull()},t=>[primaryKey({columns:[t.owner,t.thread,t.ns,t.id]})]);
export const writes=sqliteTable('tcg_writes',{owner:text('owner').notNull(),thread:text('thread').notNull(),ns:text('ns').notNull(),checkpoint:text('checkpoint').notNull(),task:text('task').notNull(),idx:integer('idx').notNull(),channel:text('channel').notNull(),payload:text('payload').notNull()},t=>[primaryKey({columns:[t.owner,t.thread,t.ns,t.checkpoint,t.task,t.idx]})]);
