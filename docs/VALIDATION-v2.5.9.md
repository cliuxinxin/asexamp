# v2.5.9 演示布局调整

- 对话结果、输入区与修改目标栏取消固定宽度上限；桌面主要内容左右留白约 20px，窄屏维持 15px。
- 输入框默认高 52px，最小 44px，允许拖动高度或滚动长文本。新会话和移动布局不再恢复到旧的 135px/115px 最小高度。
- 缩小输入框外部和工具栏间距；正文、结果表格字号及业务流程不变。
- `npm run build` 通过 TypeScript 和 Vite 生产构建；保留已有 Mermaid 大分块提示。
- 仅调整布局，不新增测试、不重跑模型或后端业务测试。
- 浏览器预览访问 http://127.0.0.1:8738 被环境拦截（net::ERR_BLOCKED_BY_CLIENT）；未完成真实浏览器视觉验收。已核查桌面、初始页与窄屏 CSS 的覆盖关系。
