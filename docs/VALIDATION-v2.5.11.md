# v2.5.11 定向验证

日期：2026-09-11

## 本次行为

- 每个唯一的澄清问题都有可采用、可编辑的建议答案。没有明确原文依据时标记为假设，不伪造引用。
- 人工确认流程仅对缺少建议的问题发起一次有容量限制的补全请求。格式错误、容量不足或请求失败时使用保守假设，并缓存结果；普通任务的 JSON 修复行为保持不变。
- 采用某条建议后收起对应问题卡片；全部采用后收起问题区的说明和采用控件，保留答案编辑区和提交按钮。采用本身不会自动提交。
- 保留手动修改和备注；清空答案后恢复对应建议，重新采用在原处补入，不重复添加。
- 轮询不清空草稿；切换任务或确认节点时重置草稿。兼容已有等待确认任务缺少建议的数据。
- 保留宽结果区、紧凑输入框和初始空页面居中输入框的布局。

## 检查结果

1. `python3 tests/test_clarification_suggestions_v2511.py -v`：13 项通过。覆盖建议规范化、引用和假设、上下文容量、缓存、取消、历史确认节点，以及实际 `WorkflowEngine.invoke_model` 包装方法的一次调用与普通任务重试行为。
2. `node --import tsx --test tests/guided-v258.test.tsx`：18 项通过。覆盖单条/全部采用、隐藏提示、保留编辑、清空后恢复、旧问题兜底、轮询和节点切换等行为。
3. `node --import tsx --test --test-name-pattern='confirmation is disabled|source confirmation' tests/workspace-smoke.test.tsx`：2 项通过。确认修改锁及来源确认交互未受影响。
4. 前端 `npm run build -- --emptyOutDir`：TypeScript 检查和 Vite 生产构建通过。现有 Mermaid 大资源分块提示仍存在。

## 验证范围

当前 Python 环境缺少应用运行依赖，后端检查使用标准库加载实际函数及包装方法并注入替身依赖；未运行完整 HTTP / LangGraph 集成流程。前端交互使用 DOM 测试；本环境浏览器对本地页面访问被阻止，未作浏览器截图验收。未连接真实模型服务，未运行全量历史测试。
