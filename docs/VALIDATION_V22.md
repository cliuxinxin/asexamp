# v2.2.0 验证记录

日期：2026-09-09。对象：本包源码及编译前端。运行环境：Linux、Python 3.12、项目锁定依赖。

## 结果

后端：208项通过（45.09秒）。前端：38项通过，TypeScript/Vite生产构建通过。启动器依赖检查通过。剩余提示为Starlette测试辅助接口弃用和Mermaid相关构建块体积提醒。

## 已执行

- 后端完整 pytest 回归，包括原有 API、网关、安全、版本编辑、诊断、启动器和新可靠流程。
- 前端组件交互测试，以及 TypeScript 检查和 Vite 生产构建。
- 使用真实本地 TCP HTTP 服务作为**测试夹具**，经生产网关适配器完成 DOCX 上传、需求分析、场景、用例、审查和可读取 XLSX 导出；逐次断言路径、X-API-Key、content 数组及仅 model/messages 两个请求字段。
- 真正启动 `start.py --no-install` 子进程，通过 HTTP 检查健康接口、预编译静态资源、模型配置读取和来源校验。
- 验证错误引用一次集中反馈并修复；超长主需求和变更文档分批；失败后重启、手动重试保留验收批次；运行中进程重启恢复；HITP 澄清与场景确认；跨批次 ADD 编号去冲突；缺失需求不能被澄清结果静默删除。
- 验证来源分类修改影响后续生成、运行配置快照、项目记忆隔离与删除、短期历史的显式限长、非选择用例类型校验。

## 不代表已完成的验证

- 未连接你的内网10.206.3.151模型服务，没有获得真实 Key；不能据本地夹具结果判断真实模型质量、吞吐或响应耗时。
- 当前云浏览器打开本地地址被客户端阻止。组件及HTTP界面加载通过，不宣称完成浏览器截图和视觉验收。
- 首次在线 pip 安装在本次环境不可用，改用已经安装的匹配依赖环境执行；启动器安装逻辑保留。Docker 构建、Windows/macOS 实机和可选 Docling/OCR 未执行。
- 大文件入口上限100MB不等于每种格式的100MB实测；现有长文档回归采用数万字符多段输入。海量页数需真实资料容量测试。
- 分批处理的结构覆盖不是语义完备证明；跨批次冲突、隐式依赖和实际用例质量需要人工核查。
- 本版使用单进程本地 SQLite。没有进行多用户鉴权、并发压测、生产高可用或 Copilot 对比评测。

测试夹具位于 tests/，生产服务不会切换到模拟模型。打包排除真实.env、数据库、上传文件、虚拟环境、node_modules和Git历史。
